apiVersion: v1
# PVC сохраняет временные ряды Prometheus между перезапусками Pod.
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: prom-data
  name: prom-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
